<%= form_for :events, :method => :get do |f| %>
<p class="tagline">Find me a co-working session at <%= f.text_field :city %></p>
<span class="inline">
  <span class="buttonblock">
	<%= f.submit 'Search', { :class => 'button' } %>
  </span>
</span>
<% end %>
<%= link_to "Looking to organize one?", new_event_path %>